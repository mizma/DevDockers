Host github.com-user
    hostname github.com
    user git
    identityfile ~/.ssh/id_ed25519_git
    # use ssh-keygen -t ed25519 -C "user@example.com"
    # set remote.origin to git@github.com-user:user/repo
